name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            ocaml: 5.4.1
            platform: linux
            arch: amd64
          - os: macos-15-intel
            ocaml: 5.4.1
            platform: darwin
            arch: amd64
          - os: macos-14
            ocaml: 5.4.1
            platform: darwin
            arch: arm64
          - os: windows-2022
            ocaml: 5.4.1
            platform: windows
            arch: amd64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml }}
      - run: opam install . --deps-only --with-test
      - run: opam exec -- dune runtest
      - name: Package Unix release
        if: runner.os != 'Windows'
        run: opam exec -- ./scripts/package-release.sh dist "${{ matrix.platform }}" "${{ matrix.arch }}"
      - name: Package Windows release
        if: runner.os == 'Windows'
        run: |
          opam exec -- dune build '@install'
          pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package-release.ps1 -OutputDir dist -Platform "${{ matrix.platform }}" -Arch "${{ matrix.arch }}"
      - uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256

  publish:
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/**/*.tar.gz
            dist/**/*.zip
            dist/**/*.sha256
          generate_release_notes: true
